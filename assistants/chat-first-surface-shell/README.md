# Chat-First Surface Shell

This is a prototype implementation of the premium chat-first Surface Assistant shell.

The assistant is the app. The first viewport is the chat workspace, not a landing page. Artifacts render on demand from inline assistant actions.

This pass intentionally avoids live model calls, backend state, deployment, and production registry wiring.
